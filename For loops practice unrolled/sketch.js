let ypos = 200;
let xpos = 80;
let xstep = 30;

function setup() {
  createCanvas(400, 400);
  noLoop();
}

function draw() {
  background(255);
  fill(50);
  noStroke();

    ellipse(xpos + (xstep * 0), ypos, 20, 20);
    ellipse(xpos + (xstep * 1), ypos, 20, 20);
    ellipse(xpos + (xstep * 2), ypos, 20, 20);
    ellipse(xpos + (xstep * 3), ypos, 20, 20);
    ellipse(xpos + (xstep * 4), ypos, 20, 20);
    ellipse(xpos + (xstep * 5), ypos, 20, 20);
    ellipse(xpos + (xstep * 6), ypos, 20, 20);
    ellipse(xpos + (xstep * 7), ypos, 20, 20);
    ellipse(xpos + (xstep * 8), ypos, 20, 20);
    ellipse(xpos + (xstep * 9), ypos, 20, 20);
    ellipse(xpos + (xstep * 10), ypos, 20, 20);
    ellipse(xpos + (xstep * 11), ypos, 20, 20);
    ellipse(xpos + (xstep * 12), ypos, 20, 20);

  }
